
// othello

// 가로 8, 세로 8
// 2차원 배열

const array = Array.from({ length: 8 }, () => Array.from({ length: 8 }, () => ""));

let blackStone = 2;
let whiteStone = 2;

const gameStart = () => {
}

const putStone = (x, y) => {
}

const checkStone = (x, y) => {
}

const calcTurn = () => {
}
